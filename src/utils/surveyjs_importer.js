if (PLATFORM === "vue") {
    import("survey-vue")
} 
if (PLATFORM === "jquery") {
    import("survey-jquery")
}
if (PLATFORM === "react") {
    import("survey-react")
}
if (PLATFORM === "angular") {
    import("survey-angular")
}
if (PLATFORM === "knockout") {
    import("survey-knockout")
}